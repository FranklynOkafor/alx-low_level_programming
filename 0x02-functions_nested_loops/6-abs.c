#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @n: the int in quetion
 * @int: function used
 *
 * Return: the abs of the int
 */

int _abs(int)
{

	if (n >= 0)
		return (n);
	else
		return (-n);
}
